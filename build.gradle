plugins {
    id 'maven-publish'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.github.SomaPrasanna4037'  
            artifactId = 'app'       
            version = '1.0.0'              
            artifact 'app-release.aar' 
        }
    }
}